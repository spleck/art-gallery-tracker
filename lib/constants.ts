export enum ArtStatus {
  OWNED = "OWNED",
  INTERESTED = "INTERESTED",
}

export enum FriendStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
}

export enum ShareType {
  OWNED = "OWNED",
  INTERESTED = "INTERESTED",
  BOTH = "BOTH",
}
